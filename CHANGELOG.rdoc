= Edge

* Preserve file mode and permissions when copying from the bundle directory. (Michael Hale)
* Add an auto_install option (default: true) to make the installer the default boot target. (Michael Hale)
